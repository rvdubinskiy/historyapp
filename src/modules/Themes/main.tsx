import colors from './colors';
import { buttons, buttonSizes, disabledButtons, pressedButtons } from './buttons';

const main = {
    colors,
    fontSizes: ['1rem', '1.2rem', '1.4rem', '1.6rem'],
    space: [0, 2, 4, 8, 10, 12],
    buttons,
    buttonSizes,
    disabledButtons,
    pressedButtons
};

export default main;
